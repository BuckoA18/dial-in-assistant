import React from "react";
import NavItem from "./NavItem";
import { Home, List, History, Plus, Settings } from "lucide-react";
import Nav from "./Nav";

const BottomNav = () => {
  return <Nav type="bottom" iconSize={22} />;
};

export default BottomNav;
